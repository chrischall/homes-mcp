# No credentials needed. Auth lives in your signed-in homes.com browser
# tab via the fetchproxy extension. This file is a marker only.
#
# Optional overrides:
# HOMES_WS_PORT=37149            # fetchproxy WebSocket port
